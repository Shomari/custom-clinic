<div class="container" ng-app="clinic">
	<div class="row">
		<ul class="nav nav-tabs col-md-6">
			<li class="active"><a data-toggle="tab" href="#doctor">Doctor's Info</a></li>
			<li><a data-toggle="tab" href="#office">Office Info</a></li>
			<li><a data-toggle="tab" href="#reminders" id="reminder-tab">Patient Reminders</a></li>
		</ul>
	</div> <!-- row -->
	<div class="tab-content">
		<div class="tab-pane active" id="doctor">
		<%= simple_form_for @collection, html: {multipart: true, id: "form"} do |m| %>
		
			<%= render partial: "collections/tabs/doctor", locals: { :m => m } %>
		</div>
		<div class="tab-pane" id="office">
			<%= render partial: "collections/tabs/office", locals: { :m => m } %>
		</div>
		<div class="tab-pane" id="reminders">
			<%= render partial: "collections/tabs/reminders", locals: { :m => m } %>
		</div>
			<%= m.button :submit, :id => "submit" %>
			<% end %>

	</div> <!-- tab content -->
</div><!--  container -->
